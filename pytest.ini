[pytest]
DJANGO_SETTINGS_MODULE = helion.settings
testpaths = tests
# A naive datetime reaching the ORM is a bug (misread as Asia/Makassar); fail loudly.
filterwarnings = error:.*received a naive datetime.*:RuntimeWarning
