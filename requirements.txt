Django==5.2.17
django_environ==0.12.0
gunicorn==23.0.0
PyYAML==6.0.1
django-mathfilters==1.0.0
django-esi==9.9.0
whitenoise==6.9.0
django-redis==5.4.0
celery[redis]==5.4.0
django-celery-beat==2.9.0
django-celery-results==2.5.1
psycopg2-binary==2.9.10
# The ESI client's transport. Pinned here because the scheduler classifies its
# errors itself: a failed spec load raises httpx, not an esi exception.
httpx==0.28.1
